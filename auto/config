#!/bin/sh

set -e
ISO_VOLUME="Subgraph live $(date +%m%d%Y)"

lb config noauto \
  --clean \
  --distribution stretch \
  --debian-installer live \
  --linux-flavours "amd64" \
  --linux-packages "linux-image linux-headers" \
  --firmware-binary true \
  --firmware-chroot true \
  --source false \
  --apt-indices false \
  --apt-recommends false \
  --memtest memtest86+ \
  --iso-publisher "Subgraph" \
  --iso-application "Subgraph OS" \
  --iso-volume "$ISO_VOLUME" \
  --archive-areas "main contrib non-free" \
  --bootappend-live "boot=live noconfig=sudo username=user user-fullname=User hostname=debian union=overlay quiet splash noautologin" \
	"${@}"
